<%=render'shared/search_jobs'%>

<div class="container">
  <h3 class="text-center text-success">Jobs that might interest you:</h3>
    <div class="my-3 p-3 bg-white rounded shadow-sm">
       <h6 class="border-bottom border-gray pb-2 mb-0">Jobs</h6>
       <%@get_jobs.each do |get_job| %>
       <div class="media text-muted pt-3">
         <% if get_job.company_detail.company_logo.attached? %>
           <%= image_tag get_job.company_detail.company_logo.variant(resize:"52x52"), height: 52, width: 52, class:"bd-placeholder-img mr-2 rounded img-border" %>
         <%else%>
         <svg class="bd-placeholder-img mr-2 rounded" width="52" height="52" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: 32x32"><title>Placeholder</title><rect fill="#007bff" width="100%" height="100%"/><text fill="#007bff" dy=".3em" x="50%" y="50%">32x32</text></svg>
         <%end%>
         <div class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
           <div class="d-flex justify-content-between align-items-center w-100">
             <strong class="text-gray-dark"><%=link_to get_job.title.titlecase, get_job%></strong>
             <a href="#">Follow</a>
           </div>
           <span class="d-block">@<%=get_job.company_detail.company_name%></span>
           <span class="badge bg-success text-light"><i class="fas fa-calendar-alt"></i> <%=time_ago_in_words(get_job.created_at) +" ago" %></span>
           <span class="badge bg-success text-light"><i class="fas fa-map-marker-alt"></i> <%=get_job.city %></span> <br> <br>
            <p class="text-justify"><%=get_job.description%></p>
         </div>
       </div>

       <%end%><!--end loop-->
     </div>
</div> <!--end container-->
