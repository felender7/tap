<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body>
    <%= render 'shared/navbar' %>
    <%= render 'shared/notices' %>
      <%= yield %>
     <div class="container"><%= render 'shared/footer' %></div>

  </body>
</html>
