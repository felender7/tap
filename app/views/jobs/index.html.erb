
<div class="container-fluid">
  <div class="shadow-sm p-3 mb-5 bg-light">
      <div class="row">
        <div class="col-md-8 text-muted">
          <h4><i class="far fa-list-alt"></i> All jobs</h4>
        </div>
        <div class="col-md-4 ">
              <%= link_to new_job_path, class: 'btn btn-primary btn-sm float-right' do %>
                <i class="fas fa-plus-square"></i> Add New job
              <% end %>
        </div>
      </div>
  </div>

<%if current_user.jobs.exists?%>
<div class="shadow-sm p-3 mb-5 bg-white rounded">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <div class="table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>File Name</th>
                <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% @jobs.each do |job| %>
                <%= content_tag :tr, id: dom_id(job), class: dom_class(job) do %>
                <% if current_user && job.user == current_user %>
                  <td><i class="far fa-file-pdf text-danger"></i> <%= job.title %></td>
                  <td></td>
                  <td></td>
                  <td></td>
                  <td><small><span class="badge bg-success "><%= link_to job do %>
                    <i class="fas fa-list-ul text-light"></i> </span>
                    <%end%> |
                    <span class="badge bg-danger "><%= link_to job, method: :delete, data: { confirm: 'Are you sure?' } do %>
                       <i class="fas fa-trash-alt text-light"></i></span>
                      <%end%>
                    </td>
                    <% end %>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

<%else%>

<div class="container-fluid">
  <div class="row">
  <div class="col-lg-12 col-md-6 jumbotron text-center text-info">
    <h3 ><i class="fas fa-file"></i> No job has been posted yet!</h3>
  </div>
  </div>
</div>

<%end%>
